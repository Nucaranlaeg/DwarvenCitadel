class Construction {
	constructor (x, y){
		this.x = x;
		this.y = y;
	}
}

const constructions = [];

function getConstructionMoveCost(x, y){
	const target = constructions.find(c => c.x == x && c.y == y);
	if 
}
